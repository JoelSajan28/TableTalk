# --- API server ---
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# --- SQL / DB (we use SQLite via SQLAlchemy) ---
SQLAlchemy==2.0.31

# --- Data & Excel ---
pandas==2.2.2
openpyxl==3.1.5      # .xlsx support
xlrd==2.0.1          # .xls support

# --- LLM client ---
requests==2.32.3     # used to call Ollama or hosted APIs

# --- Frontend (Streamlit app) ---
streamlit==1.36.0

# --- Optional helpers ---
rapidfuzz==3.9.6     # (optional) fuzzy matching for table detection
python-dotenv==1.0.1 # load env vars in dev
